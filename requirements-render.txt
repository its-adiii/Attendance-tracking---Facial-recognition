# AuraTrack Render-Optimized Requirements
# This file uses versions that work well on Render's Linux environment

# Core Flask and Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
gunicorn==21.2.0

# Computer Vision - Headless version (no GUI dependencies)
opencv-python-headless==4.8.1.78

# Image Processing - Use older stable version
Pillow==9.5.0

# Numerical Computing
numpy==1.24.4

# Database and Configuration
configparser==5.3.0
python-dateutil==2.8.2

# Security
cryptography==41.0.4

# Database (PostgreSQL for Render)
psycopg2-binary==2.9.7

# Optional: Performance (can be removed if not needed)
redis==4.6.0
cachetools==5.3.1

# Testing and Development (can be removed for production)
pytest==7.4.2
pytest-flask==1.2.0
black==23.7.0
flake8==6.0.0
